# Singleton

Thread-safe Singleton metaclass (extends ABCMeta). Ensures a class has only one instance with a global access point. Used by `ApplicationConfig` and `UserStore`.

::: warriorfit.logic.singleton
    options:
      members_order: source
      show_source: true
