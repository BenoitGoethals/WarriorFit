# Cluster

Derived MFFT scoring cluster. Paratroopers map to `COMBAT` (age/gender-neutral thresholds); everyone else maps to `ENABLER` (age-bracketed thresholds). Resolved via the `cluster` property on `ServiceMen`.

::: warriorfit.core.cluster
    options:
      members_order: source
      show_source: true
      show_docstring_attributes: true
