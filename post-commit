#!/bin/sh
# post-commit hook
# Place this file in: .git/hooks/post-commit
# Then run: chmod +x .git/hooks/post-commit

YAML_FILE="version.yaml"
REPO_ROOT=$(git rev-parse --show-toplevel)
YAML_PATH="$REPO_ROOT/$YAML_FILE"

# Get current date
DATE=$(date '+%Y-%m-%d %H:%M:%S')

# Get current version from yaml, default to 0.0.0 if not found
if [ -f "$YAML_PATH" ]; then
  CURRENT_VERSION=$(grep -oP 'version:\s*\K[0-9]+\.[0-9]+\.[0-9]+' "$YAML_PATH")
else
  CURRENT_VERSION="0.0.0"
fi

# If no version found, set to 0.0.0
if [ -z "$CURRENT_VERSION" ]; then
  CURRENT_VERSION="0.0.0"
fi

# Split version into major.minor.patch
MAJOR=$(echo $CURRENT_VERSION | cut -d. -f1)
MINOR=$(echo $CURRENT_VERSION | cut -d. -f2)
PATCH=$(echo $CURRENT_VERSION | cut -d. -f3)

# Increment patch version
PATCH=$((PATCH + 1))

# New version
NEW_VERSION="$MAJOR.$MINOR.$PATCH"

# Total commit count
COMMIT_COUNT=$(git rev-list --count HEAD)

# Short commit hash
COMMIT_HASH=$(git rev-parse --short HEAD)

# Write updated yaml file
cat > "$YAML_PATH" <<EOF
# Auto-generated by post-commit hook
# Do not edit manually

version: $NEW_VERSION
date: "$DATE"
commit_count: $COMMIT_COUNT
commit_hash: $COMMIT_HASH
EOF

# Stage and commit the yaml file (without triggering the hook again)
cd "$REPO_ROOT"
git add "$YAML_FILE"
git commit --no-verify -m "chore: update version to $NEW_VERSION [$DATE]"

echo "✓ Version updated to $NEW_VERSION"
