# Translator

Translation lookup backing the `t()` helper — resolves message keys to the active locale's strings.

::: warriorfit.i18n.translator
    options:
      members_order: source
      show_source: true
