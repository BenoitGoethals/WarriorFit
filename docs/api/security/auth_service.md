# Auth Service

Handles user authentication using Argon2id one-way hashing. Provides password hashing and verification with non-blocking `asyncio.to_thread`.

::: warriorfit.security.auth_service
    options:
      members_order: source
      show_source: true
