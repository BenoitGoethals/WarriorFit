# Cross Repository

Database operations for `Cross` events and `Runner` participants: CRUD, runner-to-cross associations, existence checks.

::: warriorfit.data.repositories.cross_repository
    options:
      members_order: source
      show_source: true
