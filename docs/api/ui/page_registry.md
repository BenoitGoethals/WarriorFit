# Page Registry

Central registry of UI pages. Each `PageSpec` declares its UI/server callables and `allowed_roles` for role-based access control.

::: warriorfit.ui.page_registry
    options:
      members_order: source
      show_source: true
